import React from 'react';
import './styles/styles.scss';
import { AppRouter } from './routes/AppRouter';

const App: React.FC = () => {
    return (
        <AppRouter />
    );
};

export default App;
